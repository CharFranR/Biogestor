La refactorización del backend prosigue, falta mostrar los datos reales del llenado cuando este se inicia, falta poder agregar correctamente las calibraciones y un boton 
para agregar las materias primeras

docker exec -it drf_backend python manage.py shell -c "from django.contrib.auth import get_user_model; from authentication.models import Profile, Permissions; User = get_user_model(); admin = User.objects.get(username='oscar'); perms, _ = Permissions.objects.get_or_create(id=1, defaults={'ApproveUsers': True, 'ViewReports': True, 'GenerateReports': True, 'ViewDashboard': True, 'ViewCalibrations': True, 'ViewInventory': True, 'ModifyInventory': True}); profile, created = Profile.objects.get_or_create(user=admin, defaults={'permissions': perms}); profile.aprobado = True; profile.rol = 'ADMIN'; profile.permissions = perms; profile.save(); print('Superusuario configurado correctamente')"